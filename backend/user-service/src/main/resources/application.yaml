server:
  port: 8081

spring:
  application:
    name: user-service

  datasource:
    url: jdbc:mysql://localhost:3306/nutrex_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: nutrex_user
    password: nutrex_pass_2024
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true

nutrex:
  jwt:
    secret: nutrex_jwt_secret_key_muy_segura_2024_produccion
    expiration: 86400000

    